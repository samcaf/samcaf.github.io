TODO.md

# Ask-assistant build tooling (local only; the served site stays dependency-free)
node_modules/
tools/.cache/

# Editor/agent scratch
.claude/

# The vendored runtime (scripts/ask/vendor/) and model weights (assets/models/)
# are committed so GitHub Pages can serve the in-browser search. To regenerate:
#   scripts/ask/vendor/  -> npm i @huggingface/transformers katex
#                           (copy dist bundles + onnxruntime .wasm)
#   assets/models/       -> Xenova/bge-small-en-v1.5 (config, tokenizer, onnx/q8)
